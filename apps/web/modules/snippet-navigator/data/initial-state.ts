import { SnippetNavigatorState } from '../types/snippet-navigator.state';

export const initialState: SnippetNavigatorState = {
  topics: [],
  selectedTopic: null,
  selectedSnippetPreview: null,
};
